$('#filter').html('<%= escape_javascript (render "filter") %>')
$('#product_list').html('<%= escape_javascript (render partial: "product", collection: @products, as: :p) %>')
$('#paginator').html('<%= escape_javascript(paginate(@products, theme: "twitter-bootstrap-3", remote: true).to_s) %>')
